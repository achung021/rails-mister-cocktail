<h1>Add doses!</h1>
<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.association :ingredient %>
  <%= f.label :description, "Enter your description here" %>
  <%= f.input :description %>
  <%= f.submit %>
<% end %>